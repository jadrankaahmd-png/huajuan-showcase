# Memory - 2026-03-11

## 📅 日期
2026年3月11日（星期三）

---

## ✅ 重要事件

### **1. Telegram 新闻存储迁移到 Redis（02:48 - 03:25）**

#### **背景**
- Vercel 无法读取本地文件系统
- 必须改用外部存储（Redis）

#### **修改内容**
1. ✅ `scripts/fetch-telegram-news.js` - 抓取后保存到 Redis
2. ✅ `app/api/telegram-news/route.ts` - 从 Redis 读取数据
3. ✅ crontab - 改用新的 Node.js 脚本（`/opt/homebrew/bin/node`）

#### **Git 提交**
```
45ea723 feat: Telegram 新闻改存到 Redis ✨
f7bc7de trigger: force Vercel redeploy 🚀
2b3893d fix: 禁用 Telegram API 缓存 🔧
```

---

### **2. Telegram API 缓存问题修复（03:20 - 03:25）**

#### **问题根源**
- Vercel 默认缓存 API 响应
- 即使 Redis 更新，API 还是返回旧数据
- 页面显示 5 小时前的旧数据

#### **修复内容**
```typescript
// 禁用 Next.js 缓存
export const revalidate = 0;

// 禁用浏览器缓存
headers: {
  'Cache-Control': 'no-store, no-cache, must-revalidate',
  'Pragma': 'no-cache',
  'Expires': '0',
}
```

#### **结果**
- ✅ 每次请求都从 Redis 读取最新数据
- ✅ 用户刷新页面能看到最新新闻
- ✅ Redis 有 91 条最新新闻

---

## 🔧 系统变更

### **crontab 更新**
```bash
# 旧的（已删除）
*/15 * * * * python3 tools/telegram_channel_scraper.py

# 新的（生效）
*/15 * * * * /opt/homebrew/bin/node scripts/fetch-telegram-news.js
```

### **Redis Key**
- `telegram:news:latest` - 存储 Telegram 新闻数据

---

## 📊 当前状态

### **Git**
```
On branch main
Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean
```

### **Redis**
```
总能力数：762 ✅
Telegram 新闻：91 条 ✅
```

### **Telegram 抓取**
```
✅ crontab 每15分钟自动抓取
✅ 保存到 Redis
✅ API 禁用缓存
```

---

## 🚨 永久规则（新增）

### **规则8：API 必须禁用缓存**
- 所有动态 API 必须添加 `export const revalidate = 0`
- 必须添加 `Cache-Control: no-store` 头
- 避免返回过时数据

### **规则9：crontab 必须使用正确的 node 路径**
- ❌ 不要用：`/path/to/venv/bin/node`
- ✅ 必须用：`/opt/homebrew/bin/node`（系统 node）

---

## 💡 学习要点

### **Vercel 缓存机制**
1. **Next.js 默认缓存**：API 路由会被缓存
2. **解决方案**：`revalidate = 0`
3. **浏览器缓存**：需要设置 `Cache-Control` 头
4. **测试方法**：直接访问 API URL 检查返回时间

### **Redis 存储迁移**
1. **原因**：Vercel 读不到本地文件
2. **方法**：使用 `@upstash/redis` 客户端
3. **验证**：从 Redis 读取后验证数据完整性

---

## 📝 下次行动

### **立即测试**
1. 访问 https://www.huajuan.news/coe/telegram-news
2. 强制刷新（Cmd+Shift+R）
3. 确认显示最新数据

### **监控**
1. 监控 Telegram 抓取稳定性
2. 监控 API 是否还有缓存问题
3. 检查 Redis 数据更新频率

---

## ✅ **17:00 - 17:20 MiniMax 模型切换**

### **背景**
- 用户想把底层模型从 GLM 换成 MiniMax
- 测试了多个 API 端点

### **执行操作**
1. ✅ 备份配置文件：`~/.openclaw/openclaw.json.bak`
2. ✅ 添加 MINIMAX_API_KEY 到 env
3. ✅ 删除 moonshot provider
4. ✅ 添加 MiniMax provider：
   - baseUrl: `https://api.minimaxi.com/anthropic`
   - api: `anthropic-messages`
   - model: `MiniMax-M2.5-highspeed`
5. ✅ 切换主模型到 `minimax/MiniMax-M2.5-highspeed`
6. ✅ 重启 Gateway 服务

### **配置变更**
```json
{
  "env": {
    "MINIMAX_API_KEY": "sk-cp-9p1-..."
  },
  "models": {
    "providers": {
      "minimax": {
        "baseUrl": "https://api.minimaxi.com/anthropic",
        "api": "anthropic-messages",
        "models": [{"id": "MiniMax-M2.5-highspeed"}]
      }
    }
  },
  "agents": {
    "defaults": {
      "model": {
        "primary": "minimax/MiniMax-M2.5-highspeed"
      }
    }
  }
}
```

### **结果**
- ✅ Gateway 已重启
- ✅ 现在使用 MiniMax M2.5 HighSpeed 模型
- ⚠️ 等待用户测试确认

---

_最后更新：2026-03-11 20:15_
_状态：✅ 模型已切换到 MiniMax_

---

## 2026-03-11 20:05 重要更新

### 架构变更

1. **移除SQLite，纯Redis架构**
   - 删除了 `data/capabilities.db`
   - 重写了 `sync-to-redis.js`（不再读取SQLite）
   - 所有能力数据存储在 Redis

2. **删除GLM相关能力**
   - 从 Redis capabilities:all 删除了6个GLM相关能力
   - 原因：不再使用智谱GLM

3. **替换AI模型：GLM → MiniMax**
   - `/api/market-analyst` 从 GLM-5 改为 MiniMax M2.5
   - 添加了 MINIMAX_API_KEY 环境变量

4. **QVeris API 禁用规则（规则10）**
   - QVeris API credits 有限，仅供第三层使用
   - 所有QVeris组件已禁用（显示"功能待开发"）
   - 只有用户明确说"使用QVeris"时才调用

5. **QVeris API 问题诊断**
   - API本身正常工作
   - `/api/market-analyst` 中的 tool_id 格式错误
   - 正确格式：`financialmodelingprep.historical_price_eod.non_split_adjusted.retrieve.v1.4c43e8ed`
   - 旧代码使用 `stock_quote` 不存在

### 页面分析结论

| 页面 | 状态 |
|------|------|
| 首页 | ✅ 保留 |
| 能力中心 | ✅ 保留 |
| 伊朗局势 | ✅ 保留，真实数据 |
| QVeris | ✅ 保留，但组件已禁用 |
| Telegram新闻 | ✅ 保留 |
| 知识库 | ✅ 保留 |
| 动态模型 | ❌ 空壳，待删除 |
| 选股推荐 | ❌ 空壳，待删除 |
| 技能中心 | ❌ 已废弃 |

### API Keys（当前有效）
- MiniMax: sk-cp-9p1-TH5Fz35_-TilqNXBx0DvMEilNApqiFc8bXho5-gLdXgruX1xttPIgUWh8t_qybgX0dZ8q86o5POKu6e4EVdsSJVu3i_uqXLgHY8cnNr6eWa8pz104Qs
- QVeris: sk-YFaMAuNb1r1qBE4_0Pr3wZYtvzqHbKL8Ths6SvWOiRU

