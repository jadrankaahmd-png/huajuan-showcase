# AGENTS.md - 花卷能力仓库指令

**版本**：1.0.0  
**最后更新**：2026-03-10  
**适用范围**：花卷第一层能力管理系统

---

## 📋 项目概览

### 核心架构

```
花卷能力仓库
├── 数据存储
│   ├── Redis（Upstash）- 唯一数据源
│   ├── SQLite（data/capabilities.db）- 备份
│   └── JSON（data/custom-capabilities.json）- 自定义能力
├── 知识库
│   ├── public/knowledge_base/ - Markdown 文件
│   └── public/knowledge_base/book-sources.json - 书籍来源
└── 脚本
    ├── scripts/add-capability.js - 添加能力
    ├── scripts/sync-to-redis.js - 同步到 Redis
    └── scripts/audit-security.js - 安全审计
```

### 核心能力

- **主能力**：642个（573 SQLite + 69 自定义）
- **知识库**：40个（36知识条目 + 4书籍）
- **子页面**：25个（10伊朗 + 9 Telegram + 6 QVeris）
- **总计**：707个能力

---

## 🔧 能力管理规范

### 必须使用的 Skill

**当能力或知识库改变时**：
```bash
# 运行 Code Change Verification
$code-change-verification
```

**检查内容**：
- ✅ `npm run sync` 是否正常
- ✅ Redis 数据完整性
- ✅ 统计数字准确性

---

### 添加新能力

**使用 `npm run add-capability`**：
```bash
npm run add-capability --name "能力名称" --description "描述" --category "分类"
```

**自动化流程**：
1. ✅ 写入 Redis（立即生效）
2. ✅ 写入 `data/custom-capabilities.json`（持久化）
3. ✅ 更新统计数字

---

### 同步到 Redis

**触发条件**：
- Git push 前（自动）
- 能力变更后（手动）

**命令**：
```bash
npm run sync
```

**检查内容**：
1. ✅ 读取 SQLite + 自定义能力
2. ✅ 合并去重
3. ✅ 写入 Redis
4. ✅ 验证数据完整性

---

## 📚 知识库管理规范

### 添加知识条目

**方式1：手动添加**
```bash
# 创建 Markdown 文件
echo "# 知识标题\n内容..." > public/knowledge_base/new-knowledge.md

# 同步到 Redis
npm run sync
```

**方式2：链接自动抓取**
```bash
# 使用自动抓取脚本
node scripts/add-knowledge-from-url.js https://example.com/article
```

**自动化流程**：
1. ✅ 抓取链接内容
2. ✅ 生成 Markdown
3. ✅ 保存到 `public/knowledge_base/`
4. ✅ 运行 `npm run sync`
5. ✅ Git 自动提交

---

### 知识库同步审计

**使用 `docs-sync`**：
```bash
$docs-sync
```

**检查内容**：
- ✅ 比较知识库条目 vs 能力库
- ✅ 识别缺失的知识条目
- ✅ 识别过时的知识条目
- ✅ 生成同步报告

---

## 🚫 永久禁止事项

### 禁止删除能力

**规则**：
- ❌ **绝对禁止**删除任何能力或知识条目
- ❌ **绝对禁止**清空 Redis 数据
- ❌ **绝对禁止**删除 `data/custom-capabilities.json`

**例外**：
- ✅ 可以标记为 `inactive`（非活跃）
- ✅ 可以归档到 `data/archived/`

---

### 禁止使用模拟数据

**规则**：
- ❌ **绝对禁止**在任何页面使用模拟数据、假数据、hardcoded 数据
- ✅ **唯一允许**：真实 API 调用
- ✅ **错误处理**：API 报错时显示"数据加载失败，请稍后重试"

**详见**：MEMORY.md 中的"永久红线规则"

---

### 禁止跳过验证

**规则**：
- ❌ **禁止**在能力变更后不运行 `npm run sync`
- ❌ **禁止**在 Git push 前不运行 pre-push 钩子
- ❌ **禁止**在发布前不运行 `final-release-review`

---

## 📊 构建和测试命令

### 日常命令

```bash
# 添加新能力
npm run add-capability

# 同步到 Redis
npm run sync

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 运行审计
node scripts/audit-security.js

# 运行 Heartbeat
node scripts/heartbeat.js

# 添加知识条目
node scripts/add-knowledge-from-url.js <URL>
```

---

### Git 操作

```bash
# 正常流程（pre-push 钩子自动运行 npm run sync）
git add .
git commit -m "feat: 新功能"
git push

# 如果 pre-push 钩子失败
# 手动同步
npm run sync
git push
```

---

## 🔒 兼容性规则

### 能力命名

- ✅ 保持能力名称唯一
- ✅ 使用英文命名
- ✅ 使用描述性名称（如 "HBM4技术竞争格局深度分析"）

---

### 分类一致性

- ✅ 使用标准分类（stock-analysis, data, automation 等）
- ✅ 新分类需要明确文档化
- ✅ 同类能力使用相同分类

---

### 描述准确性

- ✅ 描述必须清晰、准确
- ✅ 包含触发条件
- ✅ 包含核心价值

---

## 📝 文档规范

### AGENTS.md 结构

```
1. 项目概览
2. 能力管理规范
3. 知识库管理规范
4. 永久禁止事项
5. 构建和测试命令
6. 兼容性规则
7. 文档规范
```

---

### SKILL.md 结构

```markdown
---
name: skill-name
description: 清晰描述（包含触发条件和核心价值）
---

# Skill 标题

## 什么时候使用
触发条件

## 执行步骤
1. 步骤1
2. 步骤2

## 输出
输出格式和示例
```

---

## 🔗 相关资源

### Redis Keys

```
capabilities:all      - 主能力列表
knowledge:items       - 知识条目
knowledge:books       - 书籍来源
iran:capabilities     - 伊朗局势
telegram:channels     - Telegram 频道
qveris:capabilities   - QVeris 能力
stats:total           - 统计数据
```

---

### API 端点

```
/api/layer1-capabilities  - 第一层能力
/api/layer2-capabilities  - 第二层能力
/api/layer3-capabilities  - 第三层能力
/api/global-stats         - 全局统计
```

---

### 重要文件

```
AGENTS.md                        - 本文件
MEMORY.md                        - 长期记忆
HEARTBEAT.md                     - 定期任务
NOW.md                           - 当前状态
data/custom-capabilities.json    - 自定义能力
scripts/add-capability.js        - 添加能力脚本
scripts/sync-to-redis.js         - 同步脚本
.git/hooks/pre-push              - Git 钩子
```

---

## ⚠️ 故障排查

### 常见问题

**问题1：npm run sync 失败**
```bash
# 检查 Redis 连接
node -e "const {Redis}=require('@upstash/redis');const r=new Redis({url:'https://valued-hamster-37498.upstash.io',token:'AZJ6AAIncDE1YzRlYzY3NzI5OTU0MWIzOTM5YzNjMWE2NDkzMTkyZHAxMzc0OTg'});r.get('stats:total').then(console.log);"

# 检查数据文件
ls -lh data/custom-capabilities.json
```

**问题2：Git push 失败**
```bash
# 手动运行同步
npm run sync

# 强制推送（谨慎使用）
git push --no-verify
```

**问题3：能力数不一致**
```bash
# 重新同步
npm run sync

# 验证 Redis 数据
curl https://valued-hamster-37498.upstash.io/get/stats:total -H "Authorization: Bearer AZJ6AAIncDE1YzRlYzY3NzI5OTU0MWIzOTM5YzNjMWE2NDkzMTkyZHAxMzc0OTg"
```

---

## 🎯 最佳实践

### 日常开发流程

```
1. 修改能力或知识库
   ↓
2. 运行 npm run add-capability（如果是新能力）
   ↓
3. 运行 npm run sync
   ↓
4. 验证数据完整性
   ↓
5. Git commit
   ↓
6. Git push（pre-push 钩子自动运行 npm run sync）
```

---

### 发布流程

```
1. 确保所有更改已同步
   ↓
2. 运行 $final-release-review
   ↓
3. 检查兼容性问题
   ↓
4. 更新版本号
   ↓
5. Git tag
   ↓
6. Git push --tags
```

---

_最后更新：2026-03-10_
_版本：1.0.0_
_状态：已创建_
